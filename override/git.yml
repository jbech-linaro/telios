# git telios configuration
configure:
  - cmd: ls
    parameter: fooo

      #  - cmd: ./configure
      #    parameter: --prefix=/tmp/git

compile:
  - cmd: make
    parameter: FOO-BAR=1 prefix=/tmp/git all doc -j4

deploy:
  #  - cmd: make
  #    parameter: install install-doc install-html

  - cmd: cat 
    parameter: find

  - cmd: banana
    parameter: find

      #  - cmd: ./configure
      #    parameter: --prefix=/tmp/git

# Apt packages
apt:
  - 22.04:
    - packages: libssl-dev libcurl4-openssl-dev gettext autoconf asciidoc


